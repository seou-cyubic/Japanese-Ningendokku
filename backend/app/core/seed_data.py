"""설치 시 넣는 초기 데이터 중, 앱 코드도 읽어야 하는 것.

`scripts/init_db.py` 와 `app/core/db_setup.py`(기동 시 빈 DB 에 계정 투입)가
같은 값을 쓴다. 한쪽에만 적으면 README 의 계정표와 실제 계정이 어긋난다.
"""

# ==========================================================================
# 관리 화면 초기 계정 (plan.md §5.2 — 3단계 권한)
#
# ⚠️ 개발용 비밀번호다. 운영 투입 전에 반드시 교체할 것.
# ==========================================================================
SEED_ADMINS = [
    # (로그인 ID, 비밀번호, 이름, 이메일, 권한)
    ("admin",   "admin1234", "システム管理者", "admin@example.jp",   "SYSTEM_ADMIN"),
    ("manager", "manager1234", "業務管理者", "manager@example.jp", "BUSINESS_ADMIN"),
    ("staff",   "staff1234", "スタッフ",   "staff@example.jp",   "STAFF"),
]

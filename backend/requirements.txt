# -*- coding: utf-8 -*-
# ↑ pip 24 미만은 이 줄이 없으면 파일을 OS 로캘(cp949 · cp932)로 읽어
#   한글 주석에서 UnicodeDecodeError 로 설치가 시작되지 않는다. 지우지 말 것.
# 건강검진 예약 시스템 — 백엔드 의존성
# 설치: pip install -r requirements.txt
#
# ※ 정확한 버전으로 고정하지 않고 하한만 지정한다.
#    같은 환경에 설치된 다른 패키지와 버전이 충돌하는 것을 피하기 위함이다.

fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
SQLAlchemy>=2.0.41,<3
alembic>=1.13,<2               # 스키마 마이그레이션 (backend/migrations)
PyMySQL>=1.1.1,<2
cryptography>=45,<48          # PyMySQL 의 caching_sha2_password 인증에 필요
pydantic>=2.12.5,<3
pydantic-settings>=2.10,<3
python-dotenv>=1.0.1,<2
itsdangerous>=2.2.0,<3        # 본인 확인 토큰 · 관리자 세션 서명
bcrypt>=4.1,<6                # 관리자 비밀번호 해시 (plan.md §13.2)
email-validator>=2.1,<3       # 이메일 주소 검증·정규화 (core/email_utils.py)
openpyxl>=3.1,<4              # 우편 일괄 입력 표의 Excel 입출력 (A-13)
python-multipart>=0.0.9       # 표 가져오기의 파일 업로드 (FastAPI UploadFile)
resend>=2.1.0                 # 이메일 발송 API
